import { EventOrderPricePipe } from './event-order-price.pipe';

describe('EventOrderPricePipe', () => {
  it('create an instance', () => {
    const pipe = new EventOrderPricePipe();
    expect(pipe).toBeTruthy();
  });
});
