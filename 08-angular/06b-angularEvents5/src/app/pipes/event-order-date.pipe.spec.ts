import { EventOrderDatePipe } from './event-order-date.pipe';

describe('EventOrderDatePipe', () => {
  it('create an instance', () => {
    const pipe = new EventOrderDatePipe();
    expect(pipe).toBeTruthy();
  });
});
