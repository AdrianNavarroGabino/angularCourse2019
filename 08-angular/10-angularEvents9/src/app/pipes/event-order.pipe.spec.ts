import { EventOrderPipe } from './event-order.pipe';

describe('EventOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new EventOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
